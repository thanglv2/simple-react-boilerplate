import { SAVE_USER } from '../constants'

import reducer from '../reducer'

// describe('userReducer', () => {
//   const initialState = {};
//   let state;
//   it('should return initialState if no type of action is provided', () => {
//     expect(reducer(initialState, {})).toEqual(initialState)
//   })

//   it('should handle SAVE_USER type', () => {
//     let payload;
//     const action = {
//       type: SAVE_USER,
//       payload,
//     }
//     expect(reducer(state, action).payload).toEqual(payload)
//   })
// })
